broadcaster.multiInstance = false
eventStore.type = REST
broadcaster.type = HTTP
broadcaster.http.blockEventsUrl=http://localhost:8082/consumer/block-event
broadcaster.http.contractEventsUrl=http://localhost:8082/consumer/contract-event

# LOGGING
logging.level.org.apache.kafka.clients.NetworkClient=error

spring.main.allow-bean-definition-overriding=true
spring.data.jpa.repositories.enabled=false
spring.liquibase.enabled=false

